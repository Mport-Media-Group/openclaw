# Canva scaffold

Register a Canva Connect app; store secrets in `~/.openclaw/.env` (`CANVA_CLIENT_ID`, `CANVA_CLIENT_SECRET`). Health script reports presence only.

Sasha intake: `~/.openclaw/workspace-sasha/content-program/INTAKE.md` (brand kit, formats). Browser profile: `canva` under `operator/browser-automation/.profiles` when using Pro login instead of API.
