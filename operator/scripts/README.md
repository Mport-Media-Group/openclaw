# Operator scripts

Put one-off maintenance scripts here (for example LaunchAgents for `ollama serve`,
backup of `~/.openclaw`, or dashboard seed data). Prefer small, idempotent
scripts; avoid storing secrets in this directory.
